{
    "name": "mysite",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "b": "pnpm --filter @mysite/backend",
        "f": "pnpm --filter @mysite/front",
        "types": "concurrently 'pnpm b types' 'pnpm f types'"
    },
    "devDependencies": {
        "concurrently": "^9.1.2",
        "prettier": "^3.5.3",
        "rimraf": "^6.0.1",
        "ts-node": "^10.9.2",
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.8.2"
    }
}