datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}
generator client {
    provider = "prisma-client-js"
}

model Idea {
  id          String @id @default(uuid())
  nick        String @unique
  name         String
  description String
  text        String
}